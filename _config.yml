title : Who is D. M.
name: 'Who is D. M.'
description: 'Projects, bits, and embedded consulting'

markdown: kramdown
highlighter: rouge
javascript_enabled: true

featured_posts_title: Unrelated
featured_posts:
  - url: '#'
    title: 'Featured post #1'
  - url: '#'
    title: 'Featured post #2'
  - url: '#'
    title: 'Featured post #3'

collections:
  bits:
    title: Bits
    default_open: true
    output: true

defaults:
  - scope:
      type: bits
    values:
      title: Bits
      permalink: /bits/:title

plugins:
  - jekyll-sitemap
